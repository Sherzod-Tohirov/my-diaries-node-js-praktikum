<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow">
        <div class="card-header bg-primary text-white">
          <h3 class="card-title mb-0">Yangi diary yarating</h3>
        </div>
        <div class="card-body">
          <form
            action="/config/"
            method="POST"
            class="needs-validation"
            novalidate
          >
            <div class="mb-3">
              <label for="imageUrl" class="form-label">Image URL</label>
              <div class="input-group">
                <span class="input-group-text"><i
                    class="bi bi-image"
                  ></i></span>
                <input
                  type="url"
                  class="form-control"
                  id="imageUrl"
                  name="imageUrl"
                  placeholder="https://example.com/image.jpg"
                  required
                />
              </div>
              <div class="form-text">Rasm uchun url kiriting</div>
            </div>

            <div class="mb-3">
              <label for="content" class="form-label">Nima haqida yozamiz ?</label>
              <textarea
                class="form-control"
                id="content"
                name="text"
                rows="6"
                placeholder="Shu yerda yozing..."
                required
              ></textarea>
            </div>

            <div class="d-grid gap-2">
              <button type="submit" class="btn btn-primary">
                <i class="bi bi-journal-plus"></i>
                Yaratish
              </button>
              <a href="/" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i>
                Ortga
              </a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

{{! Add this script at the bottom of your form }}
<script>
  // Form validation (function () { 'use strict' var forms =
  document.querySelectorAll('.needs-validation')
  Array.prototype.slice.call(forms).forEach(function (form) {
  form.addEventListener('submit', function (event) { if (!form.checkValidity())
  { event.preventDefault() event.stopPropagation() }
  form.classList.add('was-validated') }, false) }) })()
</script>